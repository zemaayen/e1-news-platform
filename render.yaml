services:
  - type: web
    name: e1-news-platform
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true

